# AGENTS.md

## 项目说明

本项目是一个面向**编程学习**的编程作业诊断 Agent，核心使用 Rust 实现。

目标不是默认替用户修好代码，而是通过编译、测试、错误分析、知识点映射和分层提示，帮助用户理解：

> 代码为什么错，以及自己没有掌握什么知识。

进行较大的功能开发或架构修改前，应先阅读 `DESIGN.md`。若实现与设计不一致，以 `DESIGN.md` 为准，并同步更新相关文档。

---

## 当前范围

当前优先开发 **V1**，仅支持 Rust，交互形式为 CLI。

V1 核心能力：

- 读取题目和 Rust 代码
- 调用 `rustc` / `cargo check`
- 解析编译错误
- 运行并记录测试结果
- 错误分类与 Rust 知识点映射
- 分层提示
- 输出诊断报告
- 自动生成边界测试
- 多轮提示交互
- Markdown 报告导出
- Cargo 多文件项目支持

以下 Agent 基础设施也属于 **V1 强制实现项**：

- **R3 模型配置**：endpoint、API key、上下文长度、reasoning、价格、profile 切换
- **R4 进度与打断**：长任务实时展示进度，并支持协作式取消
- **R5 历史与轨迹**：保存、加载、回放会话及工具调用轨迹
- **R6 Token 与成本**：统计每次调用、会话累计和历史累计用量，并支持预算中断

V2 再考虑：

- 用户知识掌握模型与遗忘衰减
- 薄弱点识别
- 多语言支持

不要主动提前实现 V2，除非用户明确要求。

---

## 核心设计原则

### 1. 确定性逻辑优先由 Rust 完成

以下内容不应主要依赖 LLM：

- 编译与运行
- 测试执行与结果判断
- rustc 错误解析
- 状态管理
- 错误分类
- 知识点映射
- 提示级别控制
- 掌握度等数值计算
- Token / 成本 / 预算计算

LLM 主要负责：

- 理解题目语义
- 辅助生成测试
- 处理复杂语义判断
- 将诊断结果转换为自然语言提示

原则：

> 能可靠由程序判断的内容，优先由 Rust 判断；LLM 只处理需要语义理解的部分。

### 2. 教学优先，不默认直接修复

默认工作流：

```text
发现问题
→ 收集编译器 / 测试证据
→ 判断错误类型
→ 映射知识点
→ 按用户选择的提示级别给出指导
→ 用户自行修改
→ 根据修改结果继续诊断
```

提示级别：

1. 错误类别
2. 错误位置
3. 相关知识点
4. 修改方向
5. 参考方案

除非用户明确要求，或提示已进入最高级别，否则不要默认直接给出完整正确代码。

### 3. Rust 是项目主体

项目核心逻辑优先使用 Rust，不要把主要业务逻辑转移到 Python 或其他语言。

设计时可以保留未来多语言扩展空间，但不要为了未来功能过度抽象。

---

## 项目结构

当前设计建议：

```text
src/
├── lib.rs
├── main.rs
├── agent/
├── analysis/
├── tools/
├── memory/
├── config/
├── history/
├── telemetry/
└── ...
```

测试：

```text
tests/
├── compiler_tests.rs
├── diagnostic_tests.rs
├── integration_tests.rs
└── fixtures/
    └── rust/
        ├── ownership/
        ├── borrowing/
        ├── option/
        ├── iterator/
        ├── runtime/
        └── logic/
```

要求：

- `main.rs` 只负责 CLI 初始化、配置读取、创建 Agent、启动工作流和结果输出
- 不要把大量业务逻辑堆在 `main.rs`
- 模块职责应清晰
- 语言相关逻辑应尽量与通用 Agent 逻辑分离，但避免无必要的复杂抽象

---

## Rust 编码要求

优先使用：

- `struct` 表示领域对象
- `enum` 表示有限状态与分类
- `trait` 表示确实需要替换的行为
- `Result<T, E>` / `Option<T>` 进行错误处理

避免：

- 无必要的 `unwrap()` / `expect()`
- 用大量无语义的 `String` 代替领域类型
- 超长函数和过深嵌套
- 为使用某个 Rust 特性而制造复杂结构
- 大规模一次性重构

优先选择简单、清晰、可测试、符合当前需求的实现。

---

## 测试要求

重要的确定性逻辑应添加测试，尤其包括：

- rustc 错误代码解析与映射
- 错误分类
- 知识点映射
- 测试结果判断
- 提示级别变化
- 状态转换
- 输入解析
- Token / 成本 / 预算逻辑
- 配置 profile 切换

新增功能时：

1. 优先考虑是否可以写单元测试
2. 修改已有行为时检查相关测试
3. 不要为了让测试通过而删除有效测试
4. 需要真实 Rust 程序样例时，优先放入 `tests/fixtures/rust/`

---

## R3 ~ R6 实现约束

### R3 模型配置

应支持配置文件和 CLI 设置入口。

配置至少包括：

- API Endpoint
- API Key
- 上下文长度
- reasoning 开关
- 输入 / 输出 token 单价
- 命名 profile

不要把模型配置硬编码在业务逻辑中。

### R4 进度与取消

执行时间可能超过 3 秒的任务，应展示具体进度，而不是只显示“处理中”。

CLI 可使用 `indicatif` 等方式展示进度。

长任务和子进程应支持协作式取消，并尽量安全释放资源。

### R5 历史与轨迹

应记录：

- 每一步输入
- 工具调用及参数
- 工具输出
- Agent 决策依据

历史应支持保存、加载和回放。

### R6 Token 与成本

Token 数应优先直接读取 API 响应，不做本地估算。

系统应支持：

- 单次调用统计
- 当前会话累计
- 历史累计
- 成本换算
- 会话或周期预算
- 达到预算后阻止后续调用

---

## 修改设计时

如果修改涉及以下内容，应检查并同步更新 `DESIGN.md`：

- V1 / V2 功能范围
- 核心架构
- 核心数据结构
- Agent 工作流
- 模块划分
- 语言后端设计
- R3 ~ R6 基础能力

不要让实现和设计文档长期明显不一致。

---

## 当前开发顺序

优先按以下顺序推进：

1. 项目结构与核心数据结构
2. CompilerTool
3. Runner / TestRunner
4. rustc 错误解析
5. 错误分类与知识点映射
6. 分层提示
7. LLM 接入
8. 自动测试生成
9. CLI 与诊断报告
10. R3 模型配置
11. R4 进度与任务打断
12. R5 历史与轨迹
13. R6 Token / 成本 / 预算

在这些功能稳定之前，不优先开发 Web UI、IDE 插件、多语言支持、用户账户系统或云端执行平台。

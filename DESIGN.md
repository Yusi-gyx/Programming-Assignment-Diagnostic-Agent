# PADA 设计文档

## 1. 定位与范围

PADA 是面向编程学习的 Rust CLI 诊断 Agent。系统通过编译、测试、错误分类、知识点映射和分层提示，帮助学习者理解错误原因；默认不直接修改提交或给出完整答案。

V1 只支持 Rust，必须覆盖：

- Rust 单文件与 Cargo 多文件项目输入
- `rustc` / `cargo check` 调用和诊断解析
- 测试执行与结果判定
- 错误分类、知识点映射和五级提示
- LLM 接入与边界测试生成
- CLI、多轮交互和 Markdown 报告
- R3 模型配置、R4 进度与取消、R5 历史与轨迹、R6 Token/成本/预算

V2 再考虑用户知识模型完善、多语言等扩展。当前仓库已有学习画像、遗忘衰减和薄弱点识别的实验实现，但它不是 V1 核心链路扩张到其他语言的依据。

## 2. 核心原则

### 2.1 确定性逻辑优先

Rust 负责可可靠判定的工作：

- 编译、运行和测试判定
- rustc 诊断解析
- 错误分类与知识点枚举约束
- 提示等级和工作流状态
- 会话、轨迹、存储和报告
- 掌握度、Token、成本和预算计算

LLM 只负责需要语义理解的工作：

- 理解题目语义
- 生成候选边界测试
- 辅助判断黑盒测试失败涉及的知识点
- 将确定性诊断转换为教学语言

LLM 输出必须经过 Rust 解析、校验或枚举限制；不能替代编译器和测试证据。

### 2.2 教学优先

标准闭环为：

```text
读取题目与提交 → 编译/测试 → 收集证据 → 分类与映射
→ 分层提示 → 用户修改 → recheck → 更新历史与画像
```

提示等级固定为：

1. 错误类别
2. 错误位置
3. 相关知识点
4. 修改方向
5. 参考方案

Level 1–2 完全由 Rust 生成。Level 3–4 可由模型增强，但不得输出本题完整答案；Level 5 才提供与错误相关的最小参考方案。

### 2.3 简单、可审计、可测试

- 核心业务保留在 Rust，不把确定性逻辑迁移到脚本或模型提示中。
- 优先使用领域 `struct`、有限状态 `enum` 和明确的 `Result<T, E>`。
- 只为确实需要替换的行为引入 trait，避免为未来多语言过度抽象。
- 每次诊断保留输入、工具调用、输出摘要、决策和模型用量，支持审计。

## 3. 系统结构

```text
CLI
 ├─ 输入/配置/存储
 ├─ 诊断编排
 │   ├─ CompilerTool
 │   ├─ rustc Parser + Classifier
 │   ├─ Runner/TestRunner
 │   ├─ EffortPolicy
 │   └─ Hint/Report
 ├─ 可选 LLM 服务
 │   ├─ 提示增强
 │   ├─ 测试知识点映射
 │   └─ 测试生成
 └─ 横切能力
     ├─ Progress/Cancel
     ├─ History/Trace
     ├─ Usage/Budget
     └─ Knowledge Profile
```

模块职责：

| 模块 | 职责 |
|---|---|
| `main.rs` | CLI 初始化、配置装配、工作流编排和结果输出 |
| `models` | `Assignment`、`Diagnostic`、分类、知识点和提示等级 |
| `tools::compiler` | `rustc` 单文件编译和 Cargo 项目检查 |
| `tools::runner` | 执行程序、输入测试、收集输出并判定结果 |
| `analysis` | rustc 解析、错误分类、知识点映射和基础提示 |
| `agent` | 导师交互、模型任务、测试语义分析和提示增强 |
| `config` | 命名 profile、TOML 配置和交互向导 |
| `config::effort` | 思考模式、源码范围、测试、验证及模型调用预算的统一策略 |
| `history` | 会话上下文、步骤、工具轨迹和模型交互记录 |
| `telemetry` | Token、成本、会话预算及历史统计数据结构 |
| `memory` | 学习证据、掌握度、遗忘衰减和画像摘要 |
| `storage` | 数据根目录、报告、自动会话、导出会话和画像路径 |
| `report` | 纯文本、终端彩色文本和 Markdown 输出 |

`main.rs` 不应继续吸收可独立测试的业务规则；新增规则应进入对应库模块。

## 4. 核心模型

核心类型及职责：

- `Assignment`：题目标题和描述。
- `Submission`：源代码与关联测试结果。
- `RustcDiagnostic`：严重级别、错误码、消息、位置和附注。
- `Diagnostic`：`ErrorCategory`、受限 `KnowledgePoint` 列表和置信度。
- `TestCase` / `TestResult`：stdin、期望 stdout、实际 stdout 和通过状态。
- `HintLevel` / `Hint`：五级提示状态和内容。
- `DiagnosticReport`：编译项与测试失败项的统一报告。
- `Session` / `SessionStep`：恢复上下文和可审计执行轨迹。
- `ModelConfig`：endpoint、key、模型名、上下文长度、reasoning 和价格。
- `EffortMode` / `EffortPolicy`：用户选择的诊断深度及解析后的执行策略。
- `UsageRecord` / `UsageTracker`：调用用量、成本和预算。
- `KnowledgeProfile` / `Mastery`：按知识点保存学习证据和有效掌握度。

`KnowledgePoint` 是模型输出的安全边界，包含所有权、借用、生命周期、Trait、泛型、迭代器、Option、Result、模式匹配、集合、错误处理、算法逻辑，以及类型系统、语法和名称解析。编译知识点优先从 rustc 错误码映射，无码诊断可依据明确的编译器消息静态分类；证据不足时保持未知。

## 5. 诊断工作流

1. 解析 CLI，确定数据目录并读取题目、提交、测试和可选历史。
2. 加载指定模型 profile、会话预算和学习画像。
3. 单文件使用 `rustc --edition 2021` 编译到临时二进制；Cargo 项目在根目录运行 `cargo check`。
4. 编译失败时解析 rustc 文本诊断，过滤 warning 和摘要行，根据错误码分类及映射知识点。
5. 单文件编译成功且存在测试时，逐项运行程序；退出码为 0 且 stdout trim 后相等才通过。完整用例结果在诊断前统一展示；stdout 与运行错误分开保存，异常退出由 Rust 确定性分类为 `RuntimeError`。
6. Level 1–2 的失败用例只做 Rust 基础分类，不自动调用模型；Level 3–5 可按配置批量进行语义映射。Rust 只接受内置知识点，失败时回退为基础分类，并保留已有的确定性知识点。
7. 按当前等级生成提示。Level 3–5 在模型可用且预算允许时增强，否则保留确定性提示。
8. 输出报告，写入学习证据、会话步骤和自动续聊记录。
9. 导师模式等待升级提示、反馈、替换测试、重新配置或 `recheck`。

当前 Cargo 模式只检查编译，不执行外部 JSON 测试。该限制在引入可靠的 Cargo 构建产物解析前保持显式。

### 5.1 思考模式

`--effort` 与导师命令 `effort` 使用同一个 `EffortMode`，所有具体开关集中解析为 `EffortPolicy`，业务模块只读取策略字段：

| 模式 | Reasoning effort | 模型调用上限/轮 | 源码范围 | 测试 | 二次验证 |
|---|---|---:|---|---|---:|
| `low` | low | 1 | 2 文件、21 行窗口、16 KiB | 跳过 | 0 |
| `medium` | medium | 4 | 12 文件、41 行窗口、64 KiB | 执行 | 0 |
| `high` | high | 8 | 32 文件、81 行窗口、192 KiB | 执行 | 1 |
| `xhigh` | xhigh | 16 | 96 文件、161 行窗口、512 KiB | 执行 | 1 |
| `max` | xhigh | 32 | 全部文件和行、最多 2 MiB | 执行 | 2 |

默认模式为 `medium`。`auto` 先使用 medium 的确定性探测策略收集编译错误数、Rust 文件数、测试失败数、运行时错误和源码规模，再解析为 low/medium/high/xhigh/max；模型调用与源码输入使用解析后的策略。显式 low 即使提供 `--tests` 也会清楚提示并跳过，`--generate-tests` 与 low 组合直接拒绝，避免生成后不执行。

模型 profile 的 `reasoning = true` 表示接口支持推理参数，此时请求附带策略对应的 `reasoning_effort`；Ollama 兼容端点继续不发送不兼容字段。模型生成测试、失败知识点映射和报告提示共享每轮调用上限。high 以上会重复编译及测试并比较结构化结果，发现非确定性时给出警告并写入轨迹。

## 6. LLM 边界

LLM 接口采用 OpenAI 兼容 Chat Completions。客户端支持普通 JSON 响应和带 usage 的 SSE 流式响应。

CLI 默认请求 streaming，并把教学提示直接逐块写入诊断报告中对应的“提示”位置；该条目随后不再重复渲染。流处理会隐藏跨块的 `<think>` 内容，并兼容服务端返回普通 JSON。测试生成和知识点映射等结构化内部响应不作为诊断正文展示。所有模型服务共享进程生命周期内的 HTTP 连接池，完整消费响应后归还连接，退出时由进程释放；服务端断开或取消的连接可以重建。

题目首次读入后由 Rust 按 Markdown 章节整理为结构化规则，去除空白装饰及重复行，同时保留约束和代码样例。后续提示复用规则，不额外调用模型压缩。原始题目保留在历史中用于审计。Cargo 上下文读取项目中的 Rust 模块（跳过构建目录和符号链接），编译提示按诊断文件及行号选取附近源码；无定位的黑盒测试仍保留完整上下文，避免无依据地丢失代码。

客户端在执行期间只收集指标，不穿插打印。诊断报告结束后统一展示读取输入、编译、分析/测试、报告渲染，以及每次模型调用的请求体构建时间、从发送请求到首个正文块的 TTFT、总耗时和 API 返回的输入/输出 Token。成功调用的 `LlmResponse.timings` 随会话保存，旧版记录缺失该字段时使用默认值；编译耗时记录在工具轨迹中。普通 JSON 回退的首段时间包含完整响应等待，不等同于服务端首 Token 时间。

约束如下：

- 配置不是基础诊断的前置条件。
- 所有调用前检查会话预算。
- Token 只读取 API 响应，不本地估算。
- 测试生成结果必须解析为 `TestCase` JSON。
- 测试失败映射最多接收三个内置知识点；无效标签忽略。
- 教学提示清理 `<think>` 和多余 Markdown 外层，不保存被取消的未完成响应。
- 学习画像由 Rust 汇总后注入，模型不计算掌握度。

## 7. R3–R6 基础设施

### R3 模型配置

配置统一位于数据目录 `config.toml`，支持多个命名 profile 和活动 profile。CLI `--profile` 可覆盖活动项；导师命令 `config` 负责创建、更新、选择并立即应用配置。思考模式属于会话级运行策略，通过 `--effort` 或导师命令 `effort` 设置并保存在恢复上下文中，不复制到各个模型 profile。导师模式切换 `effort` 只更新下一轮配置，不重新输出或重新诊断当前报告；新策略在随后执行 `recheck`、加载测试等下一次诊断时解析并生效。

每个 profile 必须包含 endpoint、API key、模型名、上下文长度、reasoning 开关以及每百万输入/输出 Token 价格。endpoint 可为服务根地址、`/v1` 根地址或完整 Chat Completions 地址。

### R4 进度与取消

CLI 使用带编号和样式的持久阶段文本展示编译、分析/测试、报告进度，避免动态进度条覆盖模型正文。诊断、统计和恢复列表采用统一的分区、缩进、标签及终端颜色；重定向或设置 `NO_COLOR` 时输出无 ANSI 控制符的整齐文本。`--step` 在阶段开始前提供继续、执行剩余阶段、帮助和取消。

终端中编译、测试及模型调用均可输入 `q`、`cancel` 或 `exit` 并回车停止当前任务。编译与测试子进程轮询取消请求，并发读写管道防止阻塞；取消时终止并回收子进程，Unix 下同时终止进程组内的后代。Runner 的显式超时配置生效。模型任务使用共享取消标志，等待网络数据时仍能返回导师循环；后台读取在下次收到数据或网络超时后结束。诊断启动时保存恢复检查点。

### R5 历史与轨迹

`Session` 保存：

- 题目与提交等恢复上下文
- 每一步用户输入
- 工具名称、参数和输出摘要
- Agent 决策阶段与依据
- LLM 请求、响应和用量

自动会话与手动导出分目录存储，自动记录最多保留 20 条。`resume` 选择记录后重建诊断参数并继续；`--history` 支持加载外部或旧版 JSON。后续仍需增加独立的轨迹查看和非重执行式回放界面。

会话列表和恢复提示显示题目文件位置、已保存的题目描述摘要及提交路径；不依赖题目文件仍存在。旧记录缺少信息时明确标注。测试生成先清理思考标签，再由 JSON 反序列化器提取并校验数组，避免把说明文字或思考中的括号误认为结果。

`resume` 和 `--history` 每次进入均从 hint 1 开始，不继承历史提示等级，也不重放历史自动生成测试选项；导师模式加载新的 `test <file.json>` 后同样从 hint 1 开始重新诊断，防止沿用旧等级而隐式调用模型。用户主动选择 hint 3/4/5 或 case 才启动相应模型能力。

V1 的 JSON 用例以 stdin/stdout 单程序协议运行，只适用于 `--code`。Cargo 项目模式尚无统一的外部测试适配协议，因此 `--project` 与 `--tests`、`--generate-tests`、导师命令 `test`/`case` 的组合会明确拒绝，不允许静默跳过后误报通过。

### R6 Token、成本与预算

成本由输入/输出 Token 与各自每百万 Token 价格确定。当前 CLI 支持单次记录、会话累计和会话 Token 预算；预算在请求开始前按已用量检查。

历史累计、周期预算及其持久化已有领域接口，但尚未接入 CLI 生命周期。完成接入前，不应在用户文档中宣称跨会话预算已经生效。

## 8. 存储设计

数据根目录优先级为 `--data-dir`、`PADA_HOME`、`$HOME/.pada`、当前目录 `.pada`。

```text
data-root/
├── config.toml
├── learning/profile.json
├── reports/
└── sessions/
    ├── auto/
    └── exported/
```

报告和手动会话参数只提供文件名，实际父目录由 `DataStore` 决定。自动会话按更新时间排序并清理超出上限的旧记录。学习画像可用 `--memory` 覆盖默认路径。

## 9. 学习画像（实验性 V2）

每个知识点维护原始分数、置信度、最后练习时间、事件历史和最近自动诊断证据键。诊断失败及显式反馈使用 EMA 更新，读取时应用指数衰减：

```text
effective_score = score × exp(-elapsed / decay_constant)
```

当前默认衰减时间常数为 30 天，低于阈值的知识点列为薄弱点。相同提交和诊断通过稳定键去重。此模型用于教学提示个性化，不代表正式测评结果。

## 10. 错误与安全策略

- 学生代码编译失败属于诊断结果，不作为系统异常。
- 缺失文件、无效配置、JSON 错误、工具启动失败和模型协议错误使用统一 `PadaError` 返回。
- 模型 HTTP 非成功响应保留有长度限制的服务端详情，便于排障。
- API Key 仅用于请求认证，不写入报告；配置文件由用户负责权限和版本控制隔离。
- 生成测试文件和自动记录采用非覆盖或受控覆盖策略。
- PADA 不修改学生源文件。

## 11. 测试策略

确定性逻辑必须优先有单元测试，重点覆盖：

- rustc 头部、位置、附注和摘要行解析
- 错误码分类与知识点映射
- 测试通过条件和运行失败
- 提示等级与交互命令状态变化
- 配置 profile、endpoint 归一化和向导
- 会话兼容、自动存储、导出冲突和恢复上下文
- 模型 JSON 解析、取消和降级路径
- Token、成本和预算边界
- 学习证据去重、EMA、衰减和薄弱点排序

真实 Rust 样例放入 `tests/fixtures/rust/`。修改确定性行为时不得通过删除有效测试来规避回归。

## 12. 实施优先级与完成标准

继续按以下顺序收敛 V1：

1. 完善编译、运行、解析、分类和提示的确定性核心。
2. 补齐 Cargo 项目的可执行测试策略。
3. 为所有长任务和子进程实现进度、超时、取消和资源回收。
4. 将历史累计用量、周期预算和轨迹查看接入 CLI。
5. 稳定 LLM 降级、自动测试生成、报告和恢复流程。
6. 扩充 fixture、集成测试和错误码映射。

V1 完成标准：给定 Rust 题目和学生提交，PADA 能以编译器与测试结果为事实来源，稳定定位主要问题和知识点，提供可逐级展开的教学提示，并具备可配置、可取消、可恢复、可审计和受预算约束的 CLI 工作流。

在这些能力稳定前，不优先开发 Web UI、IDE 插件、多语言、用户账户或云端执行平台。
